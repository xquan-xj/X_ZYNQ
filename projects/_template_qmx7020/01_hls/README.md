# 01_hls

Optional Vitis HLS 2020.2 stage.

Use this directory only when the project contains C/C++ algorithm acceleration that should become a Vivado IP. Exported IP should be referenced from the Vivado stage through `ip_repo_paths`.
